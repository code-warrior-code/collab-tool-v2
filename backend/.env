PORT=5000
JWT_SECRET=change_this_to_a_long_random_secret_key
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173

# Render Postgres URL (only needed on Render, leave blank for local SQLite)
# DATABASE_URL=postgres://user:pass@host/dbname
